name: openai-sdk-agent
channels:
  - https://repo.anaconda.com/pkgs/main
  - https://repo.anaconda.com/pkgs/r
dependencies:
  - pip
  - pip:
      - openai-agents
      - asyncio
      - fastapi
      - uvicorn
prefix: ~/miniconda3/envs/openai-sdk-agent
